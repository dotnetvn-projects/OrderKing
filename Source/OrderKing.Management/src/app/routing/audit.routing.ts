import { AuditComponent } from '../presentation/main/audit/audit.component';
import { AuthGuard } from '../framework/framework.auth.guard';

export const AuditRoutes = [
    { path: '', component: AuditComponent}
];
